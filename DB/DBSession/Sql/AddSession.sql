/**
 * @file AddSession.sql
 * inserts a session into %Session table
 * @author  Till Uhlig
 * @param string \$values the input data, e.g. 'a=1, b=2'
 * @result -
 */
 
INSERT INTO `Session` SET $values