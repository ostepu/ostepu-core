/**
 * @file SetExerciseSheet.sql
 * inserts an exercise sheet into %ExerciseSheet table
 * @author  Till Uhlig
 * @param string $values the input data, e.g. 'a=1, b=2'
 * @result -
 */
 
INSERT INTO ExerciseSheet SET $values